#include "monty.h"

/**
 * nop - this opcode implements nop
 * @stack: this is the head of the stack
 * @line_num: the line number
 * Return: nothing.
 */

void nop(stack_t **stack, unsigned int line_num)
{
	(void)stack;
	(void)line_num;
}
